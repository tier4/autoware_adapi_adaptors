## autoware_adapi_adaptors

This repository split mirrors `main` branch of `autowarefoundation/autoware_core:/api/autoware_adapi_adaptors` into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files.
